{
  "name": "@openclaw/a2a",
  "version": "0.1.0",
  "description": "OpenClaw A2A Protocol extension for agent-to-agent communication",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "tsc --outDir dist --module commonjs --target ES2022 --esModuleInterop --resolveJsonModule src/*.ts",
    "build:clean": "rm -rf dist && tsc --outDir dist --module commonjs --target ES2022 --esModuleInterop --resolveJsonModule src/*.ts && cp index.ts dist/index.ts"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "@types/node": "^20.0.0"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
